package model.locations;

public class Start extends Location{
  public Start(String description, String name, String[] items, String visibleLocations) {
    super(description, name, items, visibleLocations);
  }
}
